# Model artifacts

The trained `.keras` model is intentionally not committed.

Create it with:

```bash
python training/train_model.py
```

This project stores model metadata in `models/model_config.json`.
